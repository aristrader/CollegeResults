Controller -
@Slf4j
@RestController
@RequestMapping(PathConstants.COLLEGES_PATH)

Entity -
@Entity(name = "College")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Table(name = "College", uniqueConstraints = {
    @UniqueConstraint(columnNames = {"College_Name", "Address"})
})

@Column(name = "College_Name", nullable = false)
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)

Repository -
interface repositoryName extends JpaRepository<DocumentEntity, String (Type of primary key)>

GlobalExceptionHandlers -
@Slf4j and @ControllerAdvice (This is used by spring to detect the global exception handlers)
