Step 1 :
1. Create the database design

Step 2 :
Spring boot initializr to create a basic project with pom.

Step 3 :
Define Basic application properties -
spring.application.name=
spring.application.version=1.0
spring.profiles.active=local
server.port=8080
#Sql config
spring.jpa.hibernate.ddl-auto=update // change after system is production ready
#spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
# DataSource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/{DB_NAME}
spring.datasource.username={user_name}
spring.datasource.password={password}
# To save logs somewhere for future reference
logging.level.org.springframework=INFO
logging.level.org.hibernate=DEBUG
logging.file.name=application.log