spring.application.name=collegeresults
spring.application.version=1.0
spring.profiles.active=local
server.port=8080

# TODO : Change the below to none after it is production ready to avoid unnecessary collapse
spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# DataSource Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/college_results_db
spring.datasource.url=jdbc:mysql://localhost:3306/college_results_db_v2
spring.datasource.username=root
spring.datasource.password=password

# TODO : Add some other open source thing for logging.
# Logging
#logging.level.org.springframework=INFO
#logging.level.org.hibernate=DEBUG
#logging.file.name=application.log

# TODO : All the files try to put them up in a configuration
# Excel Handling (if needed, configure file paths or other properties)
# excel.upload.directory=/path/to/upload/directory