spring.application.name=collegeresults
spring.application.version=1.0
spring.profiles.active=local
server.port=8080

# This below is set to validate only, this will help make sure that the application only starts when the database is in line with the application logic
spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

# DataSource Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/college_results_db
spring.datasource.url=jdbc:mysql://localhost:3306/college_results_db_v2
spring.datasource.username=root
spring.datasource.password=password
